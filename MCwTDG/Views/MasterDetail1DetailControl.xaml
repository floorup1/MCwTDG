<UserControl
    x:Class="MCwTDG.Views.MasterDetail1DetailControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <ScrollViewer Name="ForegroundElement" VerticalScrollMode="Enabled" HorizontalAlignment="Stretch" Padding="12,0">
        <StackPanel HorizontalAlignment="Left">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,8,0,0">
                <TextBlock
                    Margin="12,0,0,0"
                    Text="{x:Bind MasterMenuItem.InfoName, Mode=OneWay}"
                    Style="{StaticResource SubheaderTextBlockStyle}" />
            </StackPanel>
            <Grid Name="block" Padding="0,0,0,0" Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
                <Image Source="/Assets/Images/background03.png" Stretch="UniformToFill"/>
                <TextBlock Text="{x:Bind MasterMenuItem.InfoText, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}"/>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
